import RestaurantList from "./RestaurantList";
import miniAppinitializer from "../../miniAppInitializer";
const Body = () => {
  return (
    <div>
      <RestaurantList />
    </div>
  );
};

export default Body;
